{
  "name": "todo-api",
  "version": "1.0.0",
  "description": "Express + Apollo GraphQL + PostgreSQL + Redis API",
  "type": "module",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@apollo/server": "^5.5.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "graphql": "^16.14.1",
    "pg": "^8.21.0",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
